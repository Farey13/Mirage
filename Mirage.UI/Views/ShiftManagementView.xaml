<UserControl x:Class="Mirage.UI.Views.ShiftManagementView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:mah="http://metro.mahapps.com/winfx/xaml/controls"
             xmlns:viewModels="clr-namespace:Mirage.UI.ViewModels"
             mc:Ignorable="d" d:DesignHeight="600" d:DesignWidth="900">
    <UserControl.DataContext>
        <viewModels:ShiftManagementViewModel/>
    </UserControl.DataContext>
    <Grid Margin="10">
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="2*"/>
            <ColumnDefinition Width="5"/>
            <ColumnDefinition Width="1*"/>
        </Grid.ColumnDefinitions>
        <GroupBox Grid.Column="0" Header="Defined Shifts">
            <DataGrid ItemsSource="{Binding Shifts}" SelectedItem="{Binding SelectedShift}" AutoGenerateColumns="False" IsReadOnly="True">
                <DataGrid.Columns>
                    <DataGridTextColumn Header="Name" Binding="{Binding ShiftName}" Width="*"/>
                    <DataGridTextColumn Header="Start Time" Binding="{Binding StartTime, StringFormat='hh:mm tt'}" Width="Auto"/>
                    <DataGridTextColumn Header="End Time" Binding="{Binding EndTime, StringFormat='hh:mm tt'}" Width="Auto"/>
                    <DataGridTextColumn Header="Grace (Hrs)" Binding="{Binding GracePeriodHours}" Width="Auto"/>
                </DataGrid.Columns>
            </DataGrid>
        </GroupBox>
        <GridSplitter Grid.Column="1" Width="5" HorizontalAlignment="Stretch" Background="LightGray"/>
        <StackPanel Grid.Column="2">
            <Button Content="Add New Shift" Command="{Binding NewShiftCommand}" Margin="0,0,0,10"/>
            <Button Content="Deactivate Selected" Command="{Binding DeleteShiftCommand}" Background="IndianRed" Foreground="White" Margin="0,0,0,10">
                <Button.Style>
                    <Style TargetType="Button" BasedOn="{StaticResource MahApps.Styles.Button}">
                        <Setter Property="IsEnabled" Value="True"/>
                        <Style.Triggers>
                            <DataTrigger Binding="{Binding SelectedShift}" Value="{x:Null}">
                                <Setter Property="IsEnabled" Value="False"/>
                            </DataTrigger>
                        </Style.Triggers>
                    </Style>
                </Button.Style>
            </Button>
            <GroupBox Header="Edit Shift" IsEnabled="{Binding IsEditing}">
                <StackPanel Margin="10">
                    <Label Content="Shift Name"/>
                    <TextBox Text="{Binding EditShiftName, UpdateSourceTrigger=PropertyChanged}" Margin="0,0,0,10"/>
                    <Label Content="Start Time"/>
                    <mah:TimePicker SelectedDateTime="{Binding EditStartTime, Mode=TwoWay}" Margin="0,0,0,10"/>
                    <Label Content="End Time"/>
                    <mah:TimePicker SelectedDateTime="{Binding EditEndTime, Mode=TwoWay}" Margin="0,0,0,10"/>
                    <Label Content="Grace Period (Hours)"/>
                    <mah:NumericUpDown Value="{Binding EditGracePeriodHours}" Minimum="0" Maximum="12" Margin="0,0,0,20"/>
                    <Button Content="Save Changes" Command="{Binding SaveShiftCommand}" Style="{DynamicResource MahApps.Styles.Button.Accent}"/>
                </StackPanel>
            </GroupBox>
        </StackPanel>
    </Grid>
</UserControl>