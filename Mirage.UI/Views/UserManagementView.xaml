<UserControl x:Class="Mirage.UI.Views.UserManagementView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:Mirage.UI.Views"
             xmlns:mah="http://metro.mahapps.com/winfx/xaml/controls"
             xmlns:viewModels="clr-namespace:Mirage.UI.ViewModels"
             mc:Ignorable="d" 
             d:DesignHeight="600" d:DesignWidth="1000">
    <UserControl.DataContext>
        <viewModels:UserManagementViewModel/>
    </UserControl.DataContext>

    <UserControl.Resources>
        <Style x:Key="StandardDataGridRowStyle" TargetType="DataGridRow" BasedOn="{StaticResource MahApps.Styles.DataGridRow}">
            <Style.Triggers>
                <Trigger Property="AlternationIndex" Value="1">
                    <Setter Property="Background" Value="#F9FAFB"/>
                </Trigger>
                <Trigger Property="IsMouseOver" Value="True">
                    <Setter Property="Background" Value="#E0F2F7"/>
                </Trigger>
                <Trigger Property="IsSelected" Value="True">
                    <Setter Property="Background" Value="{DynamicResource MahApps.Brushes.Accent}"/>
                    <Setter Property="Foreground" Value="{DynamicResource MahApps.Brushes.IdealForegroundColor}"/>
                </Trigger>
            </Style.Triggers>
        </Style>
    </UserControl.Resources>

    <Grid Margin="20">
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="2*"/>
            <ColumnDefinition Width="20"/>
            <ColumnDefinition Width="1*"/>
        </Grid.ColumnDefinitions>

        <Grid Grid.Column="0">
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="*"/>
            </Grid.RowDefinitions>
            <StackPanel Orientation="Horizontal" Margin="0,0,0,10">
                <Button Content="Create New User" Command="{Binding CreateUserCommand}"/>
                <Button Content="Manage Roles" Margin="10,0,0,0" Command="{Binding ManageRolesCommand}"/>
            </StackPanel>
            <GroupBox Grid.Row="1" Header="Users">
                <DataGrid ItemsSource="{Binding Users}"
                          SelectedItem="{Binding SelectedUser}"
                          RowStyle="{StaticResource StandardDataGridRowStyle}"
                          AutoGenerateColumns="False" IsReadOnly="True" AlternationCount="2">
                    <DataGrid.Columns>
                        <DataGridTextColumn Header="User ID" Binding="{Binding UserID}" Width="Auto"/>
                        <DataGridTextColumn Header="Username" Binding="{Binding Username}" Width="*"/>
                        <DataGridTextColumn Header="Full Name" Binding="{Binding FullName}" Width="2*"/>
                    </DataGrid.Columns>
                </DataGrid>
            </GroupBox>
        </Grid>

        <StackPanel Grid.Column="2">
            <StackPanel.Style>
                <Style TargetType="StackPanel">
                    <Setter Property="IsEnabled" Value="True"/>
                    <Style.Triggers>
                        <DataTrigger Binding="{Binding SelectedUser}" Value="{x:Null}">
                            <Setter Property="IsEnabled" Value="False"/>
                        </DataTrigger>
                    </Style.Triggers>
                </Style>
            </StackPanel.Style>

            <GroupBox Header="Manage Selected User">
                <StackPanel Margin="10">
                    <TextBlock Text="{Binding SelectedUser.FullName, FallbackValue='No user selected', TargetNullValue='No user selected'}" FontWeight="Bold"/>
                    <TextBlock Text="{Binding SelectedUser.Username, StringFormat='Username: {0}'}" Margin="0,5,0,0"/>
                    <Label Content="Current Roles:" Margin="0,10,0,0"/>
                    <ListBox ItemsSource="{Binding SelectedUserRoles}" DisplayMemberPath="RoleName" Height="80" Margin="0,5,0,0"/>
                </StackPanel>
            </GroupBox>

            <GroupBox Header="Assign New Role" Margin="0,10,0,0">
                <StackPanel Margin="10">
                    <Label Content="Select a Role:"/>
                    <ComboBox ItemsSource="{Binding AllRoles}" SelectedItem="{Binding SelectedRoleToAssign}" DisplayMemberPath="RoleName" Margin="0,0,0,10"/>
                    <Button Content="Assign Role" Command="{Binding AssignRoleCommand}" Style="{DynamicResource MahApps.Styles.Button.Accent}"/>
                </StackPanel>
            </GroupBox>

            <Button Content="Reset Password" Margin="0,20,0,0" Command="{Binding ResetPasswordCommand}"/>
        </StackPanel>
    </Grid>
</UserControl>