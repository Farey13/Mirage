<UserControl x:Class="Mirage.UI.Views.TaskManagementView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:mah="http://metro.mahapps.com/winfx/xaml/controls"
             xmlns:converters="clr-namespace:Mirage.UI.Converters"
             xmlns:viewModels="clr-namespace:Mirage.UI.ViewModels"
             mc:Ignorable="d" 
             d:DesignHeight="600" d:DesignWidth="900"
             Loaded="TaskManagementView_OnLoaded">

    <UserControl.Resources>
        <converters:BooleanToVisibilityConverter x:Key="BooleanToVisibilityConverter"/>
    </UserControl.Resources>

    <Grid Margin="10">
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="2*"/>
            <ColumnDefinition Width="5"/>
            <ColumnDefinition Width="1.2*"/>
        </Grid.ColumnDefinitions>

        <GroupBox Grid.Column="0" Header="Daily Task Definitions">
            <DataGrid ItemsSource="{Binding Tasks}" AutoGenerateColumns="False" IsReadOnly="True"
                      SelectedItem="{Binding SelectedTask}"
                      RowStyle="{StaticResource StandardDataGridRowStyle}"
                      MouseDoubleClick="DataGrid_MouseDoubleClick">
                <DataGrid.Columns>
                    <DataGridTextColumn Header="Task Name" Binding="{Binding TaskName}" Width="2*"/>
                    <DataGridTextColumn Header="Schedule" Binding="{Binding ScheduleType}" Width="*"/>
                    <DataGridTextColumn Header="Value" Binding="{Binding ScheduleValue}" Width="*"/>
                    <DataGridTextColumn Header="Shift ID" Binding="{Binding ShiftID}" Width="*"/>
                    
                    <!-- Active Toggle Column -->
                    <DataGridTemplateColumn Header="Active" Width="70">
                        <DataGridTemplateColumn.CellTemplate>
                            <DataTemplate>
                                <CheckBox IsChecked="{Binding IsActive, Mode=OneWay}" 
                                          Command="{Binding DataContext.ToggleActiveCommand, RelativeSource={RelativeSource AncestorType=UserControl}}"
                                          CommandParameter="{Binding}"
                                          HorizontalAlignment="Center"/>
                            </DataTemplate>
                        </DataGridTemplateColumn.CellTemplate>
                    </DataGridTemplateColumn>

                    <!-- Actions Column -->
                    <DataGridTemplateColumn Header="Actions" Width="100">
                        <DataGridTemplateColumn.CellTemplate>
                            <DataTemplate>
                                <StackPanel Orientation="Horizontal">
                                    <Button Content="Edit" 
                                            Background="#007bff" 
                                            Foreground="White"
                                            Padding="8,2" Margin="0,0,5,0"
                                            Command="{Binding DataContext.SelectTaskCommand, RelativeSource={RelativeSource AncestorType=UserControl}}"
                                            CommandParameter="{Binding}"/>
                                    <Button Content="Del" 
                                            Background="IndianRed" 
                                            Foreground="White"
                                            Padding="8,2"
                                            Command="{Binding DataContext.DeleteTaskCommand, RelativeSource={RelativeSource AncestorType=UserControl}}"
                                            CommandParameter="{Binding}"
                                            ToolTip="Soft Delete"/>
                                </StackPanel>
                            </DataTemplate>
                        </DataGridTemplateColumn.CellTemplate>
                    </DataGridTemplateColumn>
                </DataGrid.Columns>
            </DataGrid>
        </GroupBox>

        <GridSplitter Grid.Column="1" Width="5" HorizontalAlignment="Stretch" Background="LightGray"/>

        <!-- Right Panel: Add New Task (when not in edit mode) or Edit Task (when in edit mode) -->
        <ScrollViewer Grid.Column="2" VerticalScrollBarVisibility="Auto">
            <StackPanel Margin="10,0,0,0">
                
                <!-- Add New Task Section (Always visible when not editing) -->
                <TextBlock Text="Add New Task" FontSize="18" FontWeight="Bold" Margin="0,0,0,15"/>

                <Label Content="Task Name"/>
                <TextBox Text="{Binding NewTaskName}" Margin="0,0,0,10"/>

                <Label Content="Assign to Shift"/>
                <ComboBox ItemsSource="{Binding Shifts}" 
                          SelectedItem="{Binding SelectedShift}"
                          DisplayMemberPath="ShiftName"
                          Margin="0,0,0,10"/>

                <Label Content="Schedule Type"/>
                <ComboBox ItemsSource="{Binding ScheduleTypes}" 
                          SelectedItem="{Binding SelectedScheduleType}"
                          Margin="0,0,0,10"/>

                <Label Content="Schedule Value (Optional)"/>
                <ComboBox ItemsSource="{Binding DaysOfWeek}" 
                          SelectedItem="{Binding ScheduleValue}"
                          mah:TextBoxHelper.Watermark="Select Day (if Weekly)"
                          Margin="0,0,0,20"/>

                <Button Content="Create Task" 
                        Command="{Binding SaveTaskCommand}" 
                        Style="{DynamicResource MahApps.Styles.Button.Accent}"/>

                <!-- Edit Task Section (Visible when IsEditMode is true) -->
                <Border Visibility="{Binding IsEditMode, Converter={StaticResource BooleanToVisibilityConverter}}" 
                        Background="#F8F9FA" Padding="15" CornerRadius="8" Margin="0,30,0,0">
                    <StackPanel>
                        <TextBlock Text="Edit Task Definition" FontSize="18" FontWeight="Bold" Margin="0,0,0,15"
                                   Foreground="#007bff"/>

                        <Label Content="Task Name"/>
                        <TextBox Text="{Binding EditTaskName, UpdateSourceTrigger=PropertyChanged}" Margin="0,0,0,10"/>

                        <Label Content="Assign to Shift"/>
                        <ComboBox ItemsSource="{Binding Shifts}" 
                                  SelectedItem="{Binding EditSelectedShift}"
                                  DisplayMemberPath="ShiftName"
                                  Margin="0,0,0,10"/>

                        <Label Content="Schedule Type"/>
                        <ComboBox ItemsSource="{Binding ScheduleTypes}" 
                                  SelectedItem="{Binding EditScheduleType}"
                                  Margin="0,0,0,10"/>

                        <Label Content="Schedule Value (Optional)"/>
                        <ComboBox ItemsSource="{Binding DaysOfWeek}" 
                                  SelectedItem="{Binding EditScheduleValue}"
                                  Margin="0,0,0,15"/>

                        <TextBlock Text="Task Visibility" FontWeight="SemiBold" Foreground="Gray" Margin="0,0,0,5"/>
                        <mah:ToggleSwitch IsOn="{Binding EditIsActive, Mode=TwoWay}" 
                                          Header="" 
                                          OnContent="Active (Generates Daily Logs)" 
                                          OffContent="Inactive (Paused)" 
                                          Margin="0,0,0,20"/>

                        <StackPanel Orientation="Horizontal" HorizontalAlignment="Right">
                            <Button Content="SOFT DELETE" 
                                    Command="{Binding SoftDeleteTaskCommand}" 
                                    Background="#dc3545" Foreground="White" 
                                    Margin="0,0,10,0" Padding="15,8"/>
                            
                            <Button Content="CANCEL" 
                                    Command="{Binding CancelEditCommand}" 
                                    Margin="0,0,10,0" Padding="15,8"/>
                            
                            <Button Content="SAVE" 
                                    Command="{Binding SaveTaskUpdateCommand}" 
                                    Style="{StaticResource MahApps.Styles.Button.Square.Accent}"
                                    Padding="20,8"/>
                        </StackPanel>
                    </StackPanel>
                </Border>
            </StackPanel>
        </ScrollViewer>
    </Grid>
</UserControl>
